/* exported chunk */
function chunk(array, size) {
  var arr = [];
  if (array.length < count) {
    return array;
  }
  for (var i = 0; i < array.length; i += size) {
    for (var x = 0; x < size; x++) {
      var _chunk = [];
      _chunk.push

    }
  }
}
